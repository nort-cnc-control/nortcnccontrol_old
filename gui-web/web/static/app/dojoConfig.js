(function(W){
    "use strict";

    W.dojoConfig = {
        async: true,
        parseOnLoad: false,
        isDebug: true,
        locale: navigator.language || navigator.userLanguage
    };
}(this)); // this === window